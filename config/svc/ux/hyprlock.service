[Unit]
PartOf=graphical-session.target
After=graphical-session.target
BindsTo=lock.target
Before=lock.target
OnSuccess=unlock.target

[Service]
ExecStart=/bin/hyprlock
KillSignal=SIGUSR1
Restart=on-failure
Slice=session.slice

[Install]
UpheldBy=lock.target
